<form method="post" action="{{ path('test_crud_entity_delete', {'id': test_crud_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test_crud_entity.id) }}">
    <button class="btn">Delete</button>
</form>
